FROM ubuntu

ARG PASSWORD
ARG HOST
ARG HTTPS=True

ENV PORT 5000
ENV CLIENT_SCRIPT client.sh
ENV TARGET_DIR /opt/crypto-lab/

WORKDIR ${TARGET_DIR}

COPY ${CLIENT_SCRIPT} .

CMD PASSWORD=${PASSWORD} ./${CLIENT_SCRIPT} ${HOST} ${PORT} ${HTTPS}
